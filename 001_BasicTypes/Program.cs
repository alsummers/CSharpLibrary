using System;

namespace _001_BasicTypes
{
    class Program
    {
        static void Main(string[] args)
        {
            int i = 0;
            short sh = 32767; //<---Highest it can go
            byte by = 255;
            string st = "Hello World!"; // strings is double quotes

            char c = 'i'; // char is single
            char num = '5';
            bool isBoolean; //declared variable
            isBoolean = true; //initialized variable

            double doub = 7.80;//
            float f = 8.75f;
            long l = 7000;

            int? n = null;

            //cw [tab][tab]

            Console.WriteLine(doub);
            Console.WriteLine(f);
            if (isBoolean)
                Console.WriteLine("BOOOOL!");

            SpeechSnythesizer speak = new
            Console.ReadLine(); ///Read line - stops console from stopping


            
        }
    }
}
